<app>
    <h3>app component: { props.title }</h3>

    <script>
        //import random from './random.tag'
        //export default { components: { random } }
        //console.log('app tag loaded.');
    </script>
</app>